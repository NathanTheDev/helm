// Seed-data placeholder only (see prisma/seed.ts) - not a real Firebase UID,
// so nothing signed in via real/emulated Firebase auth will ever match it.
export const DEFAULT_USER_ID = "default-user";
